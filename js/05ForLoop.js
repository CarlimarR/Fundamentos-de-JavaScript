// bucle en javascript

// for

for (let i = 0; i < 10; i++) {
    console.log(i);
}

// recorrer un array
// se define el arreglo
const fruits = ["apple", "banana", "cherry"];

// esto es la variable que se define para recorrer el array
for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i]);
}   

// el ++ es para incrementar el valor de i